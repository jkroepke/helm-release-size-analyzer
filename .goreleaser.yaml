# yaml-language-server: $schema=https://goreleaser.com/static/schema.json

version: 2

builds:
  - id: "helm-release-size-analyzer"
    main: ./cmd/helm-release-size-analyzer
    binary: helm-release-size-analyzer
    goos:
      - windows
      - darwin
      - linux
    goarch:
      - amd64
      - arm64
    goamd64:
      - v2
    mod_timestamp: '{{ .CommitTimestamp }}'
    env:
      - CGO_ENABLED=0
    flags:
      - -trimpath
    ldflags:
      - >-
        -s -w
        -X github.com/jkroepke/helm-release-size-analyzer/internal/version.Version={{.Version}}
        -X github.com/jkroepke/helm-release-size-analyzer/internal/version.Revision={{.Commit}}
        -X github.com/jkroepke/helm-release-size-analyzer/internal/version.Branch={{.Branch}}
        -X github.com/jkroepke/helm-release-size-analyzer/internal/version.BuildDate={{.Date}}

archives:
  - id: helm-release-size-analyzer
    ids:
      - helm-release-size-analyzer
    formats: ['tar.xz']
    files:
      - LICENSE.txt

checksum:
  name_template: "checksums.txt"

report_sizes: true

metadata:
  mod_timestamp: "{{ .CommitTimestamp }}"

gomod:
  proxy: true

release:
  draft: false
  replace_existing_draft: true
  prerelease: auto

changelog:
  use: github-native

sboms:
  - artifacts: archive
